{
  "name": "registry-contract",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "lint": "solhint \"contracts/**/*.sol\"",
    "flatten": "truffle-flattener ./contracts/Whitelist.sol > flattened/Whitelist.sol",
    "compile": "rm -rf build && truffle compile",
    "deploy": "rm -rf build && truffle migrate --reset --compile-all",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lauren Garcia <lauren.garcia@gmail.com> (https://github.com/laurengarcia)",
  "dependencies": {
    "@0x/contracts-exchange": "2.1.11"
  },
  "devDependencies": {
    "truffle-flattener": "^1.4.0"
  }
}
